---
type: programming-style
title: "39. Sliding Window"
description: "ループ処理や個別オブジェクトへの条件分岐を排除し、インデックス行列によってデータを窓幅と歩幅で切り出し、一括して活性化関数などを適用するスタイル。"
resource: "../../39-sliding-window/main.ts"
tags:
  - "exercises-in-style"
  - "programming-style"
  - "machine-learning"
timestamp: "2026-06-23T22:45:00+09:00"
---

# 39. Sliding Window (スライディングウィンドウ)

## 制約 (Constraints)
- **フラットなデータ配列の利用**: データ（在庫数、カート内数量、価格など）は個別のオブジェクトや複雑なデータ構造ではなく、1次元のフラットな数値シーケンス（配列）として表現する。
- **ループや個別if-else判定の排除**: カートの数量判定や割引適用可否などのビジネスルール判定において、明示的な `for/while` ループや `if-else` による個別要素チェックを行わない。
- **スライディングウィンドウの適用**: 一定の窓幅（Window Size）と歩幅（Stride）で部分配列を切り出すためのインデックスマップ（インデックス行列）をあらかじめ生成する。
- **一括演算と活性化関数の適用**: 抽出されたウィンドウ行列に対し、重みベクトルとの積（線形結合）や活性化関数（ステップ関数など）を一括適用して状態判定を行う。

## コードの特徴・解説
- `getSlidingWindowIndices` 関数で、データの長さに応じたウィンドウインデックスの二次元配列を生成します。例えば、[在庫, カート, 要求] という3つの特徴量を1セットとして処理するため、ウィンドウサイズ `3`、ストライド `3` のインデックス（`[[0, 1, 2], [3, 4, 5], ...]`）を作ります。
- `applyWindow` によって、フラットなデータ配列からインデックス行列を用いてデータを窓単位で抽出（スライディングウィンドウ処理）します。
- `checkStockAvailability` では、抽出された各窓に対して重み `[1, -1, -1]`（現在の在庫数 - カート内数量 - 要求数量）とバイアス `0` で内積を計算し、結果にステップ関数を適用することで、ループや個別の if 条件を使用せずに全商品の在庫判定を一括して予測・決定しています。
- 小計の計算や割引適用の判定でも、同様に価格と数量をウィンドウサイズ `2`, ストライド `2` で切り出して一括計算を行い、合計金額に重み `[1]` とバイアス `-3000` を適用する単層ネットワークで割引適用を決定しています。

## 自分なりの消化・考察 (Personal Insights)
- **理解のポイント**:
  - 本スタイルは、画像処理や音声処理、自然言語処理のCNN（畳み込みニューラルネットワーク）における「畳み込み層」へのデータ入力準備やカーネル適用の基礎となる考え方です。
  - プログラム側で明示的なループを書くのではなく、インデックスのマップを作成してデータを切り出し、行列演算に落とし込むことで、GPUやベクトル演算ユニットによる並列高速処理（バッチ処理）を行いやすい構造になっています。
- **難しかった点 / 気づき**:
  - ビジネスロジックをオブジェクト指向や手続き型の `if (stock < cart + req)` のような単純な記述から、フラットな配列とスライディングウィンドウ、重み演算に変換する必要がありました。
  - ストライドとウィンドウサイズを特徴量のセットに合わせることで、オブジェクトの属性値の評価を「配列上の隣接関係とインデックス選択」に翻訳できる点が非常に面白いアプローチです。
- **実務への応用**:
  - 時系列分析や、ログ監視におけるスライディングウィンドウ平均（移動平均）やアノマリ検出のバッチ処理など、データの局所的パターンを効率的に検出・集計するタスクに最適です。
  - Webアプリケーションでも、一定のシーケンス（ユーザー行動のタイムラインなど）から特定の連続パターンを検出するフィルターロジックなどに適用できます。

## 関連リンク・参考資料
- [crista/exercises-in-programming-style/39-sliding-window/](https://github.com/crista/exercises-in-programming-style/tree/master/39-sliding-window)
